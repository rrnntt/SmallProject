#include "Formula/Variable.h"
//#include "Expression.h"

namespace Formula
{

void Variable::setExpression(boost::shared_ptr<Expression> expr)
{
//  IVariable_ptr tst = expr->eval();
//  if (typeid(tst.get()) != typeid(this))
//  {
//    throw std::runtime_error("Trying to set expression with wrong type of return variable");
//  }
//  m_expr = expr;
}

}// Formula
